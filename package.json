{
  "name": "emulate-setup",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "git clean -xdfq 'node_modules/*' '**/node_modules/*' '**/dist/*'",
    "compose": "(docker network create devcontainer-proxy || true) && (docker volume create devcontainer-opencode-config || true) && (docker volume create devcontainer-opencode-auth || true) && docker compose -f docker-compose.yaml up --build -d",
    "start": "bun index.ts",
    "format": "oxfmt",
    "build": "bun build --target bun --outdir dist index.ts && bun run cp -r ./node_modules/emulate/dist/fonts ./dist"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "emulate": "^0.9.0",
    "oxfmt": "^0.62.0",
    "typescript": "7.0.2"
  },
  "patchedDependencies": {
    "emulate@0.9.0": "patches/emulate@0.9.0.patch"
  }
}
