{
  "name": "emulate-setup",
  "type": "module",
  "private": true,
  "scripts": {
    "compose": "(docker network create devcontainer-proxy || true) && (docker volume create devcontainer-opencode-config || true) && (docker volume create devcontainer-opencode-auth || true) && docker compose -f docker-compose.yaml up --build -d",
    "start": "bun index.ts"
  },
  "dependencies": {
    "emulate": "^0.4.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.46.0",
    "typescript": "6.0.2"
  },
  "patchedDependencies": {
    "emulate@0.4.1": "patches/emulate@0.4.1.patch"
  }
}
